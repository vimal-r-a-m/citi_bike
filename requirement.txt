# Dashboard
streamlit==1.38.0
streamlit-autorefresh==1.0.1
plotly==5.24.1
pandas==2.2.2

# Batch leg (DuckDB, MinIO/S3, dbt)
duckdb==1.1.0
boto3==1.35.24
dbt-core==1.8.7
dbt-duckdb==1.8.3

# Streaming leg (GBFS producer, Postgres sink)
requests==2.32.3
kafka-python==2.0.2
psycopg2-binary==2.9.9

# Orchestration (only needed where Airflow runs outside the official Docker image)
apache-airflow==2.9.3